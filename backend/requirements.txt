fastapi>=0.115.0
uvicorn[standard]>=0.30.0
openai>=1.0.0
httpx>=0.27.0
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.20.0
pydantic>=2.0.0
python-dotenv>=1.0.0
sse-starlette>=2.0.0
python-multipart>=0.0.9
pypdf>=4.0.0
python-docx>=1.1.0
pymupdf>=1.24.0
pdfplumber>=0.11.0
paddleocr>=3.0.0
paddlepaddle>=3.0.0
json-repair>=0.30.0
jieba>=0.42.1
rank-bm25>=0.2.2
numpy>=1.24.0
langgraph>=1.2.0
pyyaml>=6.0

# ── 可选依赖（默认不强制安装）────────────────────────────
# PostgreSQL + pgvector：仅当使用 PostgreSQL 时安装
# asyncpg>=0.29.0
# pgvector>=0.3.0
#
# BGE-M3 GGUF / BGE-reranker-base：用户自选下载本地模型后安装
# llama-cpp-python
# FlagEmbedding
#
# PDF 扫描件 OCR（体积较大，按需安装）
# paddleocr>=2.7.0
# paddlepaddle>=2.6.0
#
# 旧版 .doc（二进制 Word 97-2003）在 Windows 上如需 Word COM 提取：
# pywin32>=306
#
# v0.2.8 可选：PaddleLayout / PP-StructureV3 版面解析
# paddlex[ocr]>=3.7.2
#
# v0.2.8 可选：pgvector 向量存储
# pgvector>=0.3.0
